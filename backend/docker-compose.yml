version: '3.8'

services:
  monitor-server:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: monitor-server
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    environment:
      - TZ=Asia/Shanghai
    restart: unless-stopped